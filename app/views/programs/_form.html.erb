<%= form_with(model: program) do |form| %>
  <% if program.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(program.errors.count, "error") %> prohibited this program from being saved:</h2>

      <ul>
        <% program.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :institution_id %>
    <%= form.text_field :institution_id %>
  </div>

  <div class="field">
    <%= form.label :program_classification_id %>
    <%= form.text_field :program_classification_id %>
  </div>

  <div class="field">
    <%= form.label :credential_level %>
    <%= form.text_field :credential_level %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
